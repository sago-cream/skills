interface:
  display_name: "PR"
  short_description: "Review and publish a draft PR to the branch remote"
  default_prompt: "Use $pr to review the finished work and publish a draft PR to this branch's remote repository."
