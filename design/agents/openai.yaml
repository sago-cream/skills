interface:
  display_name: "Design"
  short_description: "Build and polish interfaces, CSS, and motion"
  default_prompt: "Use $design to improve this interface using the project's existing styles and relevant visual or motion guidance."

policy:
  allow_implicit_invocation: true
